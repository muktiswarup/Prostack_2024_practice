const num=777
if(num>=100 && num <=999){
    console.log('the no is a 3 digit no.')
}else {
    console.log ('the no is not a 3 digit no...')
}

//npm init -y
// to tqake the nput the from the user then u have to take prompt_sync module
//require('prompt-sync)